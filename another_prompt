--- VERY HIGH PRIORITY
fait en sorte de pouvoir upload un avatar (Servir un cdn en local via fichier upload sur le serveur dans un dossier à côté de la codebase)

cdn comme sa

http<remote>/cdn/<avatar>/<id of the account>.<extension>

pouvoir gérer dans le code proprement les routes cdn . FU (futur usage/futurproof)
http<?s>://<root host>/cdn/<x>/<key>.<fileTypeAllowed>

Extension authorisé ONLY:
jpg/jpeg/webp/png

BLACKLISTE SYSTÈMATHIQUEMENT LE RESTE.
Pas que un detect typeFile avec l'extension.
Logique propre de détection du fichier *magic byte, looking bytes, etc*

Changer l 'avatar à plusieurs règles:
Rate-limit per change / per account de:
5 fois maximum en 2 heures
(window time: 2 hours, max count 5)
file length maxi: 5MB

SYSTEM DE CHANGEMENT DE MDP:
Donc avoir une upload box, propre, adapative etc

---- MEDIUM PRIORITY
ADAPTATION MOBILE:
GÉRER LES MODALS, LES BOXES, LES BOARDING, LES CHAT BOXES AUSSI POUR PETIT ÉCRAN, ADAPTER LEUR WEBVIEW PORTAGE, DIALOG BOX, CONFIRMATON, TRAY CHANNELS SIDE LIST... 
DRAG AND SNAP LIKE PHONES, SCROLLING FOR OPENGING/EXITING ETC..

--- LOW PRIORITY
Système de service mails aussi pour les comptes (optionel) pour recovery passwords, dans le page de login:
  - Recover password with emails. smtp server,

  --- NEW NEW BUG:
  when uploading new pfp, actually it uploading the file without click needing into save button.
  the normal flow that i want is. uploading , updating the preview, cancel button: keep the current one, re-freshing the previw.
  save -> saving and pushing the pfp to the api

--- bugs:
when disabling attachment sendability: the text "Message" placeholder nto the composer chat are too tigh to the left border, make a fallback padding when no attachment button

make in the settigns: an sub category: changing key, admiting TOTP key/passkey for security

--- bugd II:
sur la page share, l' avatar de l'utilisateur  ne s'affiche pas -> fix sa

--- improvements:
éviter de ask h24 ollama pour ces modèles quand un utilisateur ce connece. c'est le backend qui sert les demande d el'utilisateurs. iL relay pas sur le ollama. Le backend vas cacher avec un TLL les modèles présent en mémire dans un objet qui est dans le backend. refresh avec un TT: toutes les minutes.
L'administrateur lui, qand il demande les modèles dams le ollama, lui, force un refresh .

--- ADMIN CENTER: SMTP CONNECTIVITY.

Make a SMTP tester in the admin center, and also a viewer of the smtp credentials (default bluring the smtp pasword OBVIOUSLY) And also a SMTP tester , sending a mail to a provided one , to tets the SMTP connectivity


--- ARCHIVED FEATURES:
a way to archive conversaitons with the contetxual menue.
When conversations are archived. it's impossible to write in . only read-only. also, when conversations are archived, they are not anymore visible in the conversations side list. They are still unarchivable and checkable, in a new sub-category options into the settigns modal : "Archiveds Chats"
